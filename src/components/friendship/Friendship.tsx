import React, { useState } from 'react';

const Friendship: React.FC<{ content: string }> = (props) => {
  console.log(props)
  return (
    <div>
      <h1>Friendship</h1>
    </div>
  );
}

export default Friendship;