import React, { useState } from 'react';

const HabitTracker: React.FC<{ content: string }> = (props) => {
  console.log(props)
  return (
    <div>
      <h1>HabitTracker</h1>
    </div>
  );
}

export default HabitTracker;